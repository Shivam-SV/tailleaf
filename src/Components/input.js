export default function inputs(colors){
    const baseStyles = {
        ".form-text": {
            width: "100%"
        }
    }
    const generator = (colorName) => {

    }

    return {
        ...baseStyles,
    }
}